<?xml version="1.0"?>
<!DOCTYPE fontconfig SYSTEM "fonts.dtd">
<fontconfig>
  <!-- 扫描 Windows 系统字体目录 -->
  <dir>c:/Windows/Fonts</dir>

  <!-- 可选：也扫描工程自带 fonts 目录（将来你可以打包字体） -->
  <dir>./assets/fonts</dir>

  <!-- 缓存目录（可选） -->
  <cachedir>./.fontcache</cachedir>
</fontconfig>
